services:
  docs:
    image: django_hans_local_docs
    container_name: django_hans_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./django_hans:/app/django_hans:z
    ports:
      - '9000:9000'
    command: /start-docs
